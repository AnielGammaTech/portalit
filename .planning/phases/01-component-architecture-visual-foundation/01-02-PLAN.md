---
phase: 01-component-architecture-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/lootit/CustomerDetailHeader.jsx
  - src/components/lootit/ReconciliationTab.jsx
  - src/components/lootit/ContractTab.jsx
  - src/components/lootit/LootITCustomerDetail.jsx
  - src/components/lootit/Pax8SubscriptionCard.jsx
  - src/components/lootit/DetailDrawer.jsx
autonomous: false
requirements: [UXRD-01, UXRD-02, UXRD-03]

must_haves:
  truths:
    - "The customer detail page has a dashboard-pro aesthetic -- dense, polished, financial dashboard feel"
    - "Typography hierarchy is visible: distinct sizes/weights for page titles, card headings, labels, large values, medium values, metadata"
    - "The header uses dark/navy top section with light content below, creating clear visual hierarchy"
    - "All status indicators use Lucide icons with consistent color coding -- zero emoji characters on the page"
    - "All existing functionality still works identically -- sync, filter, review, dismiss, upload, drawer, edit rule, map line item"
  artifacts:
    - path: "src/components/lootit/CustomerDetailHeader.jsx"
      provides: "Dashboard-pro header with dark/navy top, health bar, integration widgets, reconciliation summary"
      contains: "bg-slate-900"
    - path: "src/components/lootit/ReconciliationTab.jsx"
      provides: "Filter bar and service card grid with dashboard-pro typography"
      contains: "text-xs font-semibold uppercase tracking-wider"
    - path: "src/components/lootit/LootITCustomerDetail.jsx"
      provides: "Tab bar restyled with dashboard-pro aesthetic"
      contains: "bg-slate-100"
  key_links:
    - from: "src/components/lootit/CustomerDetailHeader.jsx"
      to: "All prop sources in LootITCustomerDetail.jsx"
      via: "Prop threading unchanged from Plan 01"
      pattern: "<CustomerDetailHeader"
    - from: "All components"
      to: "Typography hierarchy"
      via: "Consistent Tailwind class patterns"
      pattern: "text-lg font-bold|text-xs.*uppercase.*tracking"
---

<objective>
Apply dashboard-pro visual styling across all extracted components -- transforming the customer detail page from its current pink-tinted design into a polished, dense, financial-dashboard aesthetic.

Purpose: This is the "restyle second" pass per D-11. With components safely extracted in Plan 01, this plan applies dramatic visual changes (per user preference for dramatic, not subtle, tweaks) using the typography hierarchy, dark/navy header contrast, and consistent spacing patterns specified in D-05 through D-10.

Output: A visually transformed customer detail page that looks like a professional financial dashboard -- dense data layout, clear hierarchy, polished typography, dark header contrast -- while maintaining 100% functional parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-component-architecture-visual-foundation/01-CONTEXT.md
@.planning/phases/01-component-architecture-visual-foundation/01-RESEARCH.md
@.planning/phases/01-component-architecture-visual-foundation/01-01-SUMMARY.md

@src/components/lootit/LootITCustomerDetail.jsx
@src/components/lootit/CustomerDetailHeader.jsx
@src/components/lootit/ReconciliationTab.jsx
@src/components/lootit/ContractTab.jsx
@src/components/lootit/Pax8SubscriptionCard.jsx
@src/components/lootit/DetailDrawer.jsx
@src/components/lootit/lootit-constants.js

<interfaces>
<!-- Typography Hierarchy from D-07 and RESEARCH.md Pattern 3 -->
Level 1 - Page/Section Titles:  text-lg font-bold text-slate-900  (or text-xl for the customer name)
Level 2 - Card Headings:        text-sm font-semibold text-slate-900
Level 3 - Section Labels:       text-xs font-semibold uppercase tracking-wider text-muted-foreground
Level 4 - Data Values (large):  text-2xl font-bold tabular-nums text-slate-900
Level 5 - Data Values (medium): text-sm font-semibold text-slate-700
Level 6 - Metadata/Secondary:   text-xs text-slate-400
Level 7 - Micro Labels:         text-[10px] uppercase tracking-wide font-medium text-muted-foreground

<!-- Status Color System from D-09 (now in lootit-constants.js) -->
Matched/OK:       emerald-500 / emerald-50
Over-billed:      amber-500 / amber-50
Under-billed:     red-500 / red-50
No data/neutral:  slate-400 / slate-50
Reviewed/dismissed: blue-500 / blue-50

<!-- Spacing from D-08 -->
Data-dense areas: p-3 or p-4 (not p-5 or p-6)
Grid layouts: gap-3 or gap-4 (consistent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle CustomerDetailHeader with dashboard-pro dark/navy header</name>
  <files>
    src/components/lootit/CustomerDetailHeader.jsx
  </files>
  <read_first>
    src/components/lootit/CustomerDetailHeader.jsx (the full file as extracted in Plan 01),
    src/components/lootit/lootit-constants.js (STATUS_COLORS for summary boxes),
    .planning/phases/01-component-architecture-visual-foundation/01-RESEARCH.md (Pattern 4: Dark/Navy Header Contrast example, lines 233-248)
  </read_first>
  <action>
    Restyle CustomerDetailHeader.jsx to apply the dashboard-pro dark/navy header pattern per D-05, D-06, D-07, D-08. This is a DRAMATIC visual change -- not a subtle tweak.

    **Header structure (top-to-bottom):**

    1. **Health bar** -- keep the existing thin bar at the very top of the card. Keep the conditional color logic (emerald >= 80%, orange >= 50%, red < 50%). Change wrapper from `bg-slate-100` to `bg-slate-800` so it's visible on the dark header.

    2. **Dark/navy header band** (D-06) -- Replace the current white `p-5` wrapper with:
       ```
       className="bg-gradient-to-r from-slate-900 to-slate-800 px-5 py-4"
       ```
       This contains:
       - Back button: Change from `bg-slate-50 border-slate-200` to `bg-white/10 hover:bg-white/20` with `text-white` icon
       - Customer name: `text-lg font-bold text-white truncate` (Level 1 typography, white on dark)
       - Subtitle: `text-xs text-slate-400` (Level 6 typography)
       - Health badge: Use semi-transparent backgrounds on dark: `bg-emerald-500/20 text-emerald-300` (>=80), `bg-amber-500/20 text-amber-300` (>=50), `bg-red-500/20 text-red-300` (<50)
       - Sync button: Change from `bg-pink-500` to `bg-white text-slate-900 hover:bg-slate-100 font-semibold shadow-sm` (white button on dark header stands out more)

    3. **Light content area** -- Below the dark header, use:
       ```
       className="bg-white px-4 py-4 space-y-4"
       ```
       Rounded bottom corners: add `rounded-b-2xl` to the outer card wrapper.

    4. **Integration widgets grid** (D-08 tighter spacing) -- Keep the existing 6-widget grid but apply:
       - Grid: `grid grid-cols-3 sm:grid-cols-6 gap-3` (gap-3 instead of gap-2)
       - Each widget: `rounded-xl px-3 py-2.5 border border-transparent {w.bg}`
       - Widget value: `text-xl font-bold tabular-nums leading-none {w.color}` (Level 4-ish, using tabular-nums)
       - Widget icon: `w-3.5 h-3.5 {w.color}` (keep existing)
       - Widget label: `text-[10px] uppercase tracking-wide font-medium text-muted-foreground` (Level 7 typography)

    5. **Reconciliation summary boxes** -- Apply dashboard-pro typography:
       - Values: `text-2xl font-bold tabular-nums` (Level 4, already close)
       - Labels: `text-[10px] font-medium uppercase tracking-wide mt-0.5` (Level 7)
       - Status icon: keep `w-4 h-4` positioning
       - Ensure the under-billed/over-billed dollar amounts use the same typography scale
       - Import `STATUS_COLORS` from `./lootit-constants` and use for the summary box colors instead of inline Tailwind classes

    6. **Overall card wrapper** -- Change from:
       ```
       className="relative bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden"
       ```
       To:
       ```
       className="relative rounded-2xl border border-slate-200 shadow-sm overflow-hidden"
       ```
       (Remove `bg-white` from wrapper since the dark band and light content area each set their own background.)

    7. **Remove pink ambient glow** -- If the `pointer-events-none absolute -top-10 ... bg-pink-400/10` element is in this component, remove it. It belongs to the old pink theme. (Note: it may be in the parent orchestrator instead -- if so, flag for removal there.)

    IMPORTANT: Do NOT change any props, prop names, or functional behavior. Only change Tailwind CSS classes and the JSX structure of the visual elements.
  </action>
  <verify>
    <automated>cd /Users/anielreyes/portalit && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CustomerDetailHeader.jsx contains `bg-gradient-to-r from-slate-900 to-slate-800` (dark header band)
    - CustomerDetailHeader.jsx contains `text-lg font-bold text-white` (customer name on dark bg)
    - CustomerDetailHeader.jsx contains `text-xs text-slate-400` (subtitle metadata)
    - CustomerDetailHeader.jsx contains `bg-white/10` (back button on dark bg)
    - CustomerDetailHeader.jsx contains `tabular-nums` (at least 2 occurrences -- widget values and summary values)
    - CustomerDetailHeader.jsx contains `text-[10px] uppercase tracking-wide font-medium` (micro labels)
    - CustomerDetailHeader.jsx does NOT contain `bg-pink-500` (old pink sync button removed)
    - CustomerDetailHeader.jsx does NOT contain `bg-pink-400/10` (old pink glow removed if present)
    - `npx vite build` completes with exit code 0
  </acceptance_criteria>
  <done>
    CustomerDetailHeader has a dark navy/slate-900 header band with white text, light content area below, dashboard-pro typography hierarchy applied to all widgets and summary boxes. The visual difference is dramatic and immediately recognizable as a financial dashboard style.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle tab bar, ReconciliationTab, ContractTab, and remaining components</name>
  <files>
    src/components/lootit/LootITCustomerDetail.jsx,
    src/components/lootit/ReconciliationTab.jsx,
    src/components/lootit/ContractTab.jsx,
    src/components/lootit/Pax8SubscriptionCard.jsx,
    src/components/lootit/DetailDrawer.jsx
  </files>
  <read_first>
    src/components/lootit/LootITCustomerDetail.jsx (for the tab bar JSX, the pink glow element, and any remaining pink-themed classes),
    src/components/lootit/ReconciliationTab.jsx (full file for filter bar and card grid styling),
    src/components/lootit/ContractTab.jsx (full file for upload zone and contract list styling),
    src/components/lootit/Pax8SubscriptionCard.jsx (full file for card styling),
    src/components/lootit/DetailDrawer.jsx (full file for drawer styling),
    src/components/lootit/lootit-constants.js (STATUS_COLORS reference)
  </read_first>
  <action>
    Apply dashboard-pro styling to the remaining components. Each change is CSS-only -- no functional changes per D-12.

    **A. LootITCustomerDetail.jsx (orchestrator) -- Tab bar and page-level elements:**

    1. Remove the pink ambient glow div: `<div className="pointer-events-none absolute -top-10 left-1/2 -translate-x-1/2 w-[600px] h-[300px] bg-pink-400/10 rounded-full blur-[100px]" />` -- delete this entirely.

    2. Restyle the tab bar from pink to slate/neutral dashboard style:
       - Wrapper: Change `bg-pink-50/60 rounded-xl p-1 shadow-[0_0_20px_-5px_rgba(236,72,153,0.1)]` to `bg-slate-100 rounded-xl p-1`
       - Active tab: Change `bg-white text-pink-600 shadow-sm` to `bg-white text-slate-900 shadow-sm`
       - Inactive tab: Change `text-slate-500 hover:text-pink-500` to `text-slate-500 hover:text-slate-700`
       - Badge on tab: Change `bg-pink-100 text-pink-600` to `bg-slate-200 text-slate-600`

    **B. ReconciliationTab.jsx -- Filter bar and layout:**

    1. Filter buttons: If they currently use pink accent, change to slate/neutral:
       - Active filter: `bg-slate-900 text-white` (or `bg-white text-slate-900 shadow-sm border border-slate-200` for a lighter feel)
       - Inactive filter: `text-slate-500 hover:text-slate-700 bg-transparent`
       - Filter counts: `text-xs text-slate-400`
    2. Section heading (if any "Issues" or count heading exists): Apply Level 3 typography: `text-xs font-semibold uppercase tracking-wider text-muted-foreground`
    3. Card grid spacing: Ensure `gap-3` or `gap-4` per D-08

    **C. ContractTab.jsx -- Upload zone and contract list:**

    1. Upload zone: Change pink accent to slate/neutral:
       - Drag active border: `border-slate-400 bg-slate-50/80` instead of `border-pink-400 bg-pink-50/80`
       - Idle border: `border-slate-200 bg-white hover:border-slate-300 hover:bg-slate-50/30` instead of pink variants
       - Upload icon container: `bg-gradient-to-br from-slate-100 to-slate-200` instead of `from-pink-100 to-rose-100`
       - Upload icon active: `bg-slate-700 shadow-lg shadow-slate-200` instead of `bg-pink-500 shadow-lg shadow-pink-200`
       - "browse files" text: `text-slate-700 font-medium` instead of `text-pink-500`
    2. Section heading "Uploaded Contracts (N)": Apply Level 3 typography: `text-xs font-semibold uppercase tracking-wider text-muted-foreground`

    **D. Pax8SubscriptionCard.jsx -- Card styling:**

    1. Ensure all text elements follow the typography hierarchy:
       - Product name: Level 2 (`text-sm font-semibold text-slate-900`)
       - PSA/Vendor count values: Level 4 (`text-2xl font-bold tabular-nums`)
       - PSA/Vendor labels: Level 7 (`text-[10px] uppercase tracking-wide font-medium text-muted-foreground`)
       - Action buttons text: Level 6 (`text-xs text-slate-400`) or keep existing small text
    2. If any pink accent colors exist, replace with the appropriate status color from STATUS_COLORS
    3. Ensure card padding is p-3 or p-4 per D-08

    **E. DetailDrawer.jsx -- Drawer styling:**

    1. If any pink accent colors exist in the drawer, replace with slate/neutral
    2. Apply typography hierarchy to drawer content:
       - Drawer title: Level 1 (`text-lg font-bold text-slate-900`)
       - Section headings in drawer: Level 3 (`text-xs font-semibold uppercase tracking-wider text-muted-foreground`)
       - Data values: Level 5 (`text-sm font-semibold text-slate-700`)
       - Metadata: Level 6 (`text-xs text-slate-400`)
    3. Keep all functional behavior (history queries, save exclusion, notes) untouched

    **F. Global sweep for remaining pink/emoji references:**

    After all restyling, grep all files in `src/components/lootit/` for:
    - Any remaining `pink-` Tailwind classes (replace with slate/neutral or the appropriate status color)
    - Any emoji characters (replace with appropriate Lucide icon per D-10)
    - Any `orange-` classes that should be `amber-` per D-09

    Exceptions: `pink-` may remain if used in a gradient that the user explicitly wants to keep (the CONTEXT.md mentions "existing pink/gradient header background can be retained or evolved"). Since we are evolving to dark/navy per D-06, pink should be fully removed.

    IMPORTANT: Do NOT change any props, handlers, state, hooks, or functional behavior. Only change Tailwind CSS classes.
  </action>
  <verify>
    <automated>cd /Users/anielreyes/portalit && npx vite build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - LootITCustomerDetail.jsx does NOT contain `bg-pink-400/10` (ambient glow removed)
    - LootITCustomerDetail.jsx does NOT contain `bg-pink-50/60` (old pink tab wrapper removed)
    - LootITCustomerDetail.jsx contains `bg-slate-100 rounded-xl p-1` (new tab bar wrapper)
    - LootITCustomerDetail.jsx contains `text-slate-900` in the active tab class (not text-pink-600)
    - ReconciliationTab.jsx contains `text-xs font-semibold uppercase tracking-wider` (Level 3 typography)
    - ContractTab.jsx does NOT contain `text-pink-500` (pink browse text removed)
    - ContractTab.jsx does NOT contain `border-pink-400` (pink drag border removed)
    - `grep -r 'pink-' src/components/lootit/` returns empty (zero pink references remaining)
    - `grep -r 'orange-' src/components/lootit/` returns empty (zero orange references, all amber per D-09)
    - DetailDrawer.jsx contains `text-lg font-bold text-slate-900` (Level 1 drawer title)
    - Pax8SubscriptionCard.jsx contains `tabular-nums` (data values use tabular numerals)
    - `npx vite build` completes with exit code 0
  </acceptance_criteria>
  <done>
    All components restyled with dashboard-pro aesthetic. Pink theme fully replaced with slate/neutral + status-color system. Typography hierarchy applied consistently: dark headings, uppercase micro-labels, bold tabular values. Tab bar is neutral slate. Upload zone is neutral. All remaining orange replaced with amber. Zero emoji characters. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard-pro visual transformation</name>
  <files>
    src/components/lootit/CustomerDetailHeader.jsx,
    src/components/lootit/ReconciliationTab.jsx,
    src/components/lootit/ContractTab.jsx,
    src/components/lootit/LootITCustomerDetail.jsx
  </files>
  <read_first>
    src/components/lootit/CustomerDetailHeader.jsx (verify dark header classes present),
    src/components/lootit/LootITCustomerDetail.jsx (verify pink glow removed, tab bar restyled)
  </read_first>
  <action>
    Human visual verification of the dashboard-pro transformation. Start the dev server and walk through every section of the customer detail page.

    What was built:
    - Dark navy/slate-900 header band with white text and semi-transparent health badge
    - Light content area below with integration widgets and reconciliation summary
    - Dashboard-pro typography hierarchy (7 levels from page titles to micro-labels)
    - Unified status color system (emerald=matched, amber=over, red=under, slate=neutral, blue=reviewed)
    - All components extracted into focused files (none exceeding 400 lines)
    - Pink theme completely removed, replaced with professional slate/neutral tones
    - All functionality preserved (sync, filter, review, dismiss, upload, drawer, edit, map)

    Verification steps:
    1. Run the dev server: `npm run dev`
    2. Navigate to LootIT and select any customer with reconciliation data
    3. Verify the header: dark navy/black gradient top, white customer name, health badge, integration widgets below, reconciliation summary boxes
    4. Verify the tab bar is neutral (no pink tones)
    5. Click through both Reconciliation and Contract tabs -- verify content renders
    6. In Reconciliation tab, test filter buttons (All, Issues, Matched, Reviewed)
    7. Click a service card to open the detail drawer -- verify it opens and displays data
    8. Click the Sync button -- verify it triggers sync with loading state
    9. In Contract tab, verify the upload zone appears (drag-and-drop area, no pink)
    10. Check that no emoji characters appear anywhere on the page
    11. Overall impression: Does this look like a professional financial dashboard? Dense, polished, clear hierarchy?
  </action>
  <verify>
    <automated>cd /Users/anielreyes/portalit && grep -r 'pink-' src/components/lootit/ | wc -l | xargs test 0 -eq && echo "PASS: zero pink references" || echo "FAIL: pink references remain"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms the page has a dashboard-pro aesthetic (dark header, light content, professional typography)
    - User confirms all functionality works (sync, filter, review, dismiss, upload, drawer)
    - User confirms zero emoji characters visible
    - User confirms zero pink tones visible
    - grep for pink- in src/components/lootit/ returns zero matches
  </acceptance_criteria>
  <done>
    User has visually verified and approved the dashboard-pro transformation. The page looks like a professional financial dashboard with clear visual hierarchy, consistent typography, and unified status colors.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/anielreyes/portalit && npx vite build` -- build completes with exit code 0
2. `grep -r 'pink-' src/components/lootit/` -- returns empty (zero pink references)
3. `grep -r 'orange-' src/components/lootit/` -- returns empty (zero orange references)
4. `grep -r 'bg-slate-900\|from-slate-900' src/components/lootit/CustomerDetailHeader.jsx` -- returns matches (dark header present)
5. `grep -c 'tabular-nums' src/components/lootit/CustomerDetailHeader.jsx` -- returns >= 2
6. `grep -c 'uppercase tracking-wi' src/components/lootit/` -- returns multiple matches across files (typography hierarchy)
7. Visual verification by user at checkpoint
</verification>

<success_criteria>
- The page has a clear dashboard-pro aesthetic with dark header, light content, and professional typography
- Typography hierarchy is consistently applied (7 levels visible across components)
- Zero pink-themed classes remain anywhere in src/components/lootit/
- Zero orange classes remain (all amber per D-09)
- Zero emoji characters on the page (all Lucide icons per D-10)
- All existing functionality works: sync, filter, review, dismiss, upload, drawer, edit rule, map line item
- Vite build passes
- User approves the visual result at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-component-architecture-visual-foundation/01-02-SUMMARY.md`
</output>
