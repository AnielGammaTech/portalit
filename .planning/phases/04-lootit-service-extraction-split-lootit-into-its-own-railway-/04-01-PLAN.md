---
phase: 04-lootit-service-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/lootit/package.json
  - apps/lootit/vite.config.js
  - apps/lootit/index.html
  - apps/lootit/tailwind.config.js
  - apps/lootit/postcss.config.js
  - apps/lootit/jsconfig.json
  - apps/lootit/components.json
  - apps/lootit/Dockerfile
  - apps/lootit/Caddyfile
  - apps/lootit/nixpacks.toml
  - apps/lootit/.dockerignore
  - apps/lootit/.gitignore
  - apps/lootit/src/main.jsx
  - apps/lootit/src/App.jsx
  - apps/lootit/src/Layout.jsx
  - apps/lootit/src/index.css
  - apps/lootit/public/favicon.svg
autonomous: true
requirements: []
decision_ids: [D-01, D-02, D-03, D-04, D-05, D-09, D-10, D-11, D-12, D-14, D-26, D-27, D-28, D-32]

must_haves:
  truths:
    - "apps/lootit/ directory exists as a self-contained Vite app"
    - "cd apps/lootit && npm run build produces apps/lootit/dist/index.html with hashed asset bundles"
    - "cd apps/lootit && npm run dev serves a React page on a free port (default 5174)"
    - "apps/lootit has its own node_modules isolated from root node_modules"
  artifacts:
    - path: "apps/lootit/package.json"
      provides: "Dependency manifest and npm scripts (dev, build, preview)"
      contains: "\"build\": \"vite build\""
    - path: "apps/lootit/vite.config.js"
      provides: "Vite config with @/* alias to ./src"
      contains: "'@': path.resolve"
    - path: "apps/lootit/index.html"
      provides: "SPA entry point"
      contains: "<div id=\"root\">"
    - path: "apps/lootit/src/main.jsx"
      provides: "React boot — mounts App into #root"
      contains: "createRoot"
    - path: "apps/lootit/src/App.jsx"
      provides: "Root app shell with providers and routes"
    - path: "apps/lootit/Dockerfile"
      provides: "Multi-stage node build + caddy serve"
      contains: "FROM caddy:2-alpine"
    - path: "apps/lootit/Caddyfile"
      provides: "Static serving with SPA fallback on :8080"
      contains: "try_files"
  key_links:
    - from: "apps/lootit/vite.config.js"
      to: "apps/lootit/src"
      via: "@/* path alias"
      pattern: "alias.*@"
    - from: "apps/lootit/src/main.jsx"
      to: "apps/lootit/src/App.jsx"
      via: "import App from './App'"
      pattern: "import App"
    - from: "apps/lootit/Dockerfile"
      to: "apps/lootit/Caddyfile"
      via: "COPY Caddyfile /etc/caddy/Caddyfile"
      pattern: "COPY Caddyfile"
---

<objective>
Scaffold the `apps/lootit/` self-contained Vite application shell — configuration files, entry points, Docker/Caddy deployment artifacts, and a minimal working App.jsx skeleton that can render a placeholder page and build green. No LootIT code copied yet (that happens in Plan 04-03).

Purpose: Establishes the isolated build target so Plan 04-03 can drop copied source code into a working app structure, and Plan 04-05 can deploy it. Enforces D-01 (apps/lootit top-level folder), D-02 (self-contained configs), D-03 (isolated node_modules), D-10 (minimal dep list), D-27 (Dockerfile + Caddy serve). Per D-05, `extensions/lootit-link/` stays at the repo root and is not touched by this phase.

Output: A buildable Vite app at apps/lootit/ that renders "LootIT app shell ready" as a placeholder and ships as a Docker image.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-CONTEXT.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-RESEARCH.md
@package.json
@vite.config.js
@jsconfig.json
@tailwind.config.js
@postcss.config.js
@components.json
@index.html
@Caddyfile

<interfaces>
The shell must use EXACTLY these versions pulled from portalit's root `package.json` (D-10 minimize surprise, and locked in RESEARCH.md Standard Stack):

Runtime deps:
- react ^18.2.0
- react-dom ^18.2.0
- @supabase/supabase-js ^2.49.1
- @tanstack/react-query ^5.84.1
- react-router-dom ^6.26.0
- sonner ^2.0.1
- lucide-react ^0.475.0
- @radix-ui/react-dialog ^1.1.6
- @radix-ui/react-tooltip ^1.1.8
- @radix-ui/react-slot ^1.1.2
- @radix-ui/react-label ^2.1.2
- class-variance-authority ^0.7.1
- clsx ^2.1.1
- tailwind-merge ^3.0.2
- date-fns ^3.6.0

Dev deps:
- vite ^6.1.0
- @vitejs/plugin-react ^4.3.4
- tailwindcss ^3.4.17
- tailwindcss-animate ^1.0.7
- autoprefixer ^10.4.20
- postcss ^8.5.3
- typescript ^5.8.2
- @types/node matching portalit
- @types/react matching portalit
- @types/react-dom matching portalit

DO NOT install: @hello-pangea/dnd, @hookform/resolvers, @stripe/*, canvas-confetti, cmdk, embla-carousel-react, framer-motion, input-otp, lodash, next-themes, react-day-picker, react-hook-form, react-hot-toast, react-leaflet, react-markdown, react-quill, react-resizable-panels, recharts, vaul, zod (unless plan 04-03 finds a transitive need — this plan ships without them).

NPM scripts (copy from portalit's package.json):
```json
"scripts": {
  "dev": "vite --port 5174",
  "build": "vite build",
  "preview": "vite preview --port 5174",
  "typecheck": "tsc -p jsconfig.json --noEmit"
}
```

Vite alias (from portalit vite.config.js):
```javascript
resolve: { alias: { '@': path.resolve(__dirname, './src') } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create config files, package.json, and entry HTML</name>
  <files>apps/lootit/package.json, apps/lootit/vite.config.js, apps/lootit/jsconfig.json, apps/lootit/tailwind.config.js, apps/lootit/postcss.config.js, apps/lootit/components.json, apps/lootit/index.html, apps/lootit/.gitignore, apps/lootit/.dockerignore, apps/lootit/public/favicon.svg</files>
  <read_first>
    - /Users/anielreyes/portalit/package.json (source for versions and scripts)
    - /Users/anielreyes/portalit/vite.config.js (alias pattern to mirror)
    - /Users/anielreyes/portalit/jsconfig.json (baseUrl + paths to mirror)
    - /Users/anielreyes/portalit/tailwind.config.js (verbatim copy, adjust content globs to local)
    - /Users/anielreyes/portalit/postcss.config.js (verbatim copy)
    - /Users/anielreyes/portalit/components.json (verbatim copy)
    - /Users/anielreyes/portalit/index.html (copy structure, update title)
    - /Users/anielreyes/portalit/public/favicon.svg (copy to apps/lootit/public/favicon.svg)
    - /Users/anielreyes/portalit/.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-RESEARCH.md (Standard Stack section + Recommended Project Structure)
  </read_first>
  <action>
Create the following files with the exact content specified:

1. `apps/lootit/package.json`:
```json
{
  "name": "lootit",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --port 5174",
    "typecheck": "tsc -p jsconfig.json --noEmit"
  },
  "dependencies": {
    "@radix-ui/react-dialog": "^1.1.6",
    "@radix-ui/react-label": "^2.1.2",
    "@radix-ui/react-slot": "^1.1.2",
    "@radix-ui/react-tooltip": "^1.1.8",
    "@supabase/supabase-js": "^2.49.1",
    "@tanstack/react-query": "^5.84.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "date-fns": "^3.6.0",
    "lucide-react": "^0.475.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.26.0",
    "sonner": "^2.0.1",
    "tailwind-merge": "^3.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "tailwindcss-animate": "^1.0.7",
    "typescript": "^5.8.2",
    "vite": "^6.1.0"
  }
}
```
Note: If portalit package.json has DIFFERENT @types/* versions, use those instead. Read portalit/package.json and match exactly.

2. `apps/lootit/vite.config.js`:
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5174,
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
  },
})
```

3. `apps/lootit/jsconfig.json` — copy verbatim from portalit root `jsconfig.json`. The baseUrl and paths should already be `"./"` and `"@/*": ["src/*"]`, which resolve correctly from apps/lootit.

4. `apps/lootit/tailwind.config.js` — copy verbatim from portalit root `tailwind.config.js`. DO NOT change content globs — they are relative (e.g., `"./index.html"`, `"./src/**/*.{js,jsx,ts,tsx}"`) and will resolve from apps/lootit at build time.

5. `apps/lootit/postcss.config.js` — copy verbatim from portalit root `postcss.config.js`.

6. `apps/lootit/components.json` — copy verbatim from portalit root `components.json`.

7. `apps/lootit/index.html`:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LootIT</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

8. `apps/lootit/.gitignore`:
```
node_modules
dist
.env
.env.local
*.log
.DS_Store
```

9. `apps/lootit/.dockerignore`:
```
node_modules
dist
.env
.env.local
*.log
.git
.DS_Store
```

10. `apps/lootit/public/favicon.svg` — copy the file byte-for-byte from `public/favicon.svg` at repo root. If that path doesn't exist, copy from `public/vite.svg` or use a minimal placeholder SVG of a circle.
  </action>
  <acceptance_criteria>
    - `test -f apps/lootit/package.json` succeeds
    - `grep -q '"name": "lootit"' apps/lootit/package.json`
    - `grep -q '"react": "\^18.2.0"' apps/lootit/package.json`
    - `grep -q '"vite": "\^6.1.0"' apps/lootit/package.json`
    - `grep -q '"build": "vite build"' apps/lootit/package.json`
    - `! grep -q 'framer-motion\|recharts\|react-quill\|leaflet\|zod' apps/lootit/package.json` (excluded deps must not appear)
    - `grep -q "'@': path.resolve" apps/lootit/vite.config.js`
    - `grep -q 'id="root"' apps/lootit/index.html`
    - `test -f apps/lootit/jsconfig.json && test -f apps/lootit/tailwind.config.js && test -f apps/lootit/postcss.config.js && test -f apps/lootit/components.json`
    - `test -f apps/lootit/public/favicon.svg`
    - `test -f apps/lootit/.gitignore && test -f apps/lootit/.dockerignore`
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/lootit/package.json && test -f apps/lootit/vite.config.js && test -f apps/lootit/index.html && test -f apps/lootit/jsconfig.json && test -f apps/lootit/tailwind.config.js && test -f apps/lootit/postcss.config.js && test -f apps/lootit/components.json && test -f apps/lootit/.gitignore && test -f apps/lootit/.dockerignore && test -f apps/lootit/public/favicon.svg && grep -q '"name": "lootit"' apps/lootit/package.json && grep -q "'@': path.resolve" apps/lootit/vite.config.js</automated>
  </verify>
  <done>All 10 config/entry files exist at the specified paths with matching content; excluded dependencies (framer-motion, recharts, quill, leaflet, zod) are absent from package.json.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create React entry, App shell, Layout, index.css, and deployment artifacts</name>
  <files>apps/lootit/src/main.jsx, apps/lootit/src/App.jsx, apps/lootit/src/Layout.jsx, apps/lootit/src/index.css, apps/lootit/Dockerfile, apps/lootit/Caddyfile, apps/lootit/nixpacks.toml</files>
  <read_first>
    - /Users/anielreyes/portalit/src/main.jsx (for boot pattern to mirror per D-11)
    - /Users/anielreyes/portalit/src/index.css (for Tailwind directives and CSS tokens to trim per D-09)
    - /Users/anielreyes/portalit/Caddyfile (verbatim copy — verified reusable per RESEARCH Pattern 3)
    - /Users/anielreyes/portalit/.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-RESEARCH.md (Pattern 3: Dockerfile for Vite + Caddy)
  </read_first>
  <action>
Create the following files with exact content:

1. `apps/lootit/src/main.jsx`:
```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

2. `apps/lootit/src/App.jsx` (PLACEHOLDER — Plan 04-03 will replace this with the full providers + routes tree):
```jsx
import Layout from './Layout.jsx'

export default function App() {
  return (
    <Layout>
      <div className="flex h-screen items-center justify-center text-2xl font-semibold text-slate-900">
        LootIT app shell ready
      </div>
    </Layout>
  )
}
```

3. `apps/lootit/src/Layout.jsx` (minimal skeleton — Plan 04-03 will wire real sidebar/header per D-14):
```jsx
export default function Layout({ children }) {
  return (
    <div className="min-h-screen bg-slate-50">
      {children}
    </div>
  )
}
```

4. `apps/lootit/src/index.css` — create a MINIMAL Tailwind-directives-only file per D-09. Do not copy the full portalit index.css; trim to:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```
Plan 04-03 will add LootIT-specific CSS tokens (shadcn CSS variables: `--background`, `--foreground`, etc.) when it copies over the components that need them. This task only needs the directives for the placeholder shell to build.

5. `apps/lootit/Dockerfile` (verbatim from RESEARCH Pattern 3):
```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
RUN npm run build

FROM caddy:2-alpine
WORKDIR /app
COPY --from=build /app/dist ./dist
COPY Caddyfile /etc/caddy/Caddyfile
EXPOSE 8080
CMD ["caddy", "run", "--config", "/etc/caddy/Caddyfile", "--adapter", "caddyfile"]
```
Note: Uses `npm install` not `npm ci` because package-lock.json may not exist in this task (created in Task 3). Railway will run `npm install` cleanly on first deploy.

6. `apps/lootit/Caddyfile` — copy VERBATIM from /Users/anielreyes/portalit/Caddyfile. Expected content:
```
{ admin off }
:8080 {
    root * /app/dist
    file_server
    try_files {path} /index.html
    @html { path / ; path /index.html }
    header @html Cache-Control "no-cache, no-store, must-revalidate"
    header @html CDN-Cache-Control "no-store"
    header @html Surrogate-Control "no-store"
    @assets { path /assets/* }
    header @assets Cache-Control "public, max-age=31536000, immutable"
}
```

7. `apps/lootit/nixpacks.toml` — create a minimal fallback in case Railway uses nixpacks instead of the Dockerfile:
```toml
[phases.setup]
nixPkgs = ['nodejs_20', 'caddy']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'caddy run --config ./Caddyfile --adapter caddyfile'
```
  </action>
  <acceptance_criteria>
    - `test -f apps/lootit/src/main.jsx`
    - `grep -q 'createRoot' apps/lootit/src/main.jsx`
    - `grep -q "import App from './App" apps/lootit/src/main.jsx`
    - `test -f apps/lootit/src/App.jsx`
    - `grep -q 'LootIT app shell ready' apps/lootit/src/App.jsx`
    - `test -f apps/lootit/src/Layout.jsx`
    - `test -f apps/lootit/src/index.css`
    - `grep -q '@tailwind base' apps/lootit/src/index.css`
    - `test -f apps/lootit/Dockerfile`
    - `grep -q 'FROM caddy:2-alpine' apps/lootit/Dockerfile`
    - `grep -q 'COPY --from=build /app/dist' apps/lootit/Dockerfile`
    - `test -f apps/lootit/Caddyfile`
    - `grep -q 'try_files' apps/lootit/Caddyfile`
    - `grep -q 'root \* /app/dist' apps/lootit/Caddyfile`
    - `test -f apps/lootit/nixpacks.toml`
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/lootit/src/main.jsx && test -f apps/lootit/src/App.jsx && test -f apps/lootit/src/Layout.jsx && test -f apps/lootit/src/index.css && test -f apps/lootit/Dockerfile && test -f apps/lootit/Caddyfile && test -f apps/lootit/nixpacks.toml && grep -q 'createRoot' apps/lootit/src/main.jsx && grep -q 'FROM caddy:2-alpine' apps/lootit/Dockerfile && grep -q 'try_files' apps/lootit/Caddyfile</automated>
  </verify>
  <done>React entry, placeholder App+Layout, minimal index.css, Dockerfile, Caddyfile, and nixpacks.toml all exist with the specified content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install dependencies and verify a green build</name>
  <files>apps/lootit/package-lock.json, apps/lootit/node_modules/ (generated), apps/lootit/dist/ (generated)</files>
  <read_first>
    - /Users/anielreyes/portalit/apps/lootit/package.json (just written in Task 1)
    - /Users/anielreyes/portalit/apps/lootit/vite.config.js (just written)
  </read_first>
  <action>
Run the following commands in order from the repo root:

```bash
cd /Users/anielreyes/portalit/apps/lootit && npm install
cd /Users/anielreyes/portalit/apps/lootit && npm run build
```

Expected outcome:
- `npm install` creates `apps/lootit/node_modules/` and `apps/lootit/package-lock.json`
- `npm run build` produces `apps/lootit/dist/index.html` and `apps/lootit/dist/assets/*.js` + `apps/lootit/dist/assets/*.css` (hashed filenames)
- Exit code 0 on both commands

If `npm install` reports peer-dep warnings for React 18, those are normal (Radix and TanStack Query both list React 18 as peer — matching). Do NOT use `--force` or `--legacy-peer-deps`.

If `npm run build` fails with a missing dependency error, add the missing package to `apps/lootit/package.json` (NOT to portalit root package.json) and re-run. Document any added package in the commit message.
  </action>
  <acceptance_criteria>
    - `test -d apps/lootit/node_modules`
    - `test -f apps/lootit/package-lock.json`
    - `test -f apps/lootit/dist/index.html`
    - `ls apps/lootit/dist/assets/*.js | head -1` returns at least one hashed .js file
    - `grep -q '<div id="root">' apps/lootit/dist/index.html`
    - `cd apps/lootit && npm run build` exits 0 when re-run (idempotent)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/anielreyes/portalit/apps/lootit && npm run build && test -f dist/index.html && ls dist/assets/*.js >/dev/null 2>&1 && grep -q 'id="root"' dist/index.html</automated>
  </verify>
  <done>Build succeeds with exit 0, dist/ contains index.html + hashed assets, node_modules is isolated under apps/lootit/.</done>
</task>

</tasks>

<verification>
1. All 17 config/source files exist under `apps/lootit/` at the paths listed in `files_modified`.
2. `cd apps/lootit && npm run build` exits 0 and produces `dist/index.html` + `dist/assets/*.{js,css}`.
3. `cd apps/lootit && npm run dev` can start a dev server on port 5174 (manual smoke — not blocking in this task since Plan 04-03 needs full code to be useful).
4. `apps/lootit/node_modules/` is fully isolated from root `node_modules/` (no symlinks).
5. Excluded dependencies (framer-motion, recharts, quill, leaflet, zod) do NOT appear in `apps/lootit/package.json` (per D-10 minimal deps).
</verification>

<success_criteria>
- Scaffold can be handed off to Plan 04-03 which will replace placeholder App.jsx with the full providers + routes tree and populate src/api, src/lib, src/hooks, src/components, src/pages.
- The build artifact `apps/lootit/dist/` is a valid SPA ready to be containerized via the Dockerfile.
- Root directory `apps/lootit` can be specified to Railway as the service Root Directory in Plan 04-05 without further changes to these files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-01-SUMMARY.md` with:
- What was scaffolded (file list by category)
- Exact dependency versions pinned (for Plan 04-03's reference)
- Build output location and size
- Any deviations from the planned dep list (e.g., peer-dep warnings, added transitive deps)
- Next-plan note: Plan 04-03 will replace apps/lootit/src/App.jsx with the full providers+routes shell and populate src/api, src/lib, src/hooks, src/components, src/pages
</output>
