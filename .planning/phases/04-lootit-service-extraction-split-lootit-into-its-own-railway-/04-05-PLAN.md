---
phase: 04-lootit-service-extraction
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified: []
autonomous: false
requirements: []
decision_ids: [D-23, D-24, D-25, D-26, D-27, D-28, D-31, D-32]
user_setup:
  - service: railway
    why: "Create new lootit-frontend Railway service in the existing PortalIT project, configure env vars, generate deployment URL, update backend CORS_ORIGIN"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Copy from existing frontend service → Variables tab"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Copy from existing frontend service → Variables tab"
      - name: VITE_API_BASE_URL
        source: "https://backend-production-58b4.up.railway.app"
      - name: VITE_PORTALIT_URL
        source: "https://portalit.gtools.io"
    dashboard_config:
      - task: "Create new service 'lootit-frontend' in PortalIT project"
        location: "Railway → PortalIT project (ID 935a26e9-0cea-4eff-a7e7-d551840fad71) → New Service → Deploy from GitHub repo → AnielGammaTech/portalit → branch feat/lootit-split → Root Directory: apps/lootit"
      - task: "Generate Railway-provided domain (lootit-frontend-production-*.up.railway.app)"
        location: "Railway → lootit-frontend service → Settings → Domains → Generate Domain"
      - task: "Update backend service CORS_ORIGIN env var to include the new Railway URL"
        location: "Railway → backend service → Variables → CORS_ORIGIN — append the lootit-frontend URL comma-separated"

must_haves:
  truths:
    - "A Railway service named 'lootit-frontend' exists in the PortalIT project"
    - "The service builds from apps/lootit/ on branch feat/lootit-split"
    - "The service has a live URL (Railway-provided or lootit-test.gtools.io)"
    - "curl <live-url>/ returns HTML containing 'id=\"root\"'"
    - "Backend CORS_ORIGIN env var includes the new Railway URL"
    - "A user signed in on portalit.gtools.io who visits the new URL is recognized as authenticated (SSO works)"
  artifacts:
    - path: "(Railway service configuration — not a local file)"
      provides: "Deployed lootit-frontend service with env vars"
  key_links:
    - from: "lootit-frontend Railway service"
      to: "apps/lootit/ on branch feat/lootit-split"
      via: "Root Directory config"
    - from: "lootit-frontend Railway service"
      to: "backend service CORS allowlist"
      via: "CORS_ORIGIN env var includes lootit-frontend URL"
---

<objective>
Create the `lootit-frontend` Railway service inside the existing PortalIT Railway project, configure its env vars, deploy from `apps/lootit/` on the `feat/lootit-split` branch, and verify the deployment end-to-end including cross-subdomain SSO against production portalit. Update backend CORS_ORIGIN to allow the new origin.

Purpose: D-26 (new service in PortalIT project), D-27 (Dockerfile + Caddy), D-28 (temporary domain first — no DNS flip yet), D-23 (shared backend), D-24 (CORS allowlist update). This plan is the first time LootIT runs as its own Railway service. The verification step proves cookie-based SSO works end-to-end before Plan 04-06 touches portalit's sidebar.

Output: A running lootit-frontend Railway service at a verified URL, with CORS allowed, with end-to-end SSO confirmed in at least Chrome. This is the phase's "big bang" — if this plan passes, the cookie adapter, the CORS config, the build, and the route shell have all been proven together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-CONTEXT.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-RESEARCH.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-03-SUMMARY.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-04-SUMMARY.md

@apps/lootit/Dockerfile
@apps/lootit/Caddyfile

<interfaces>
Railway project reference:
- Project name: PortalIT
- Project ID: 935a26e9-0cea-4eff-a7e7-d551840fad71
- Repo: AnielGammaTech/portalit
- Branch for phase 4: feat/lootit-split
- Existing services:
  - frontend (portalit.gtools.io)
  - customer-portal (customer.portalit.gtools.io)
  - backend (backend-production-58b4.up.railway.app)

New service to create:
- Name: lootit-frontend
- Root Directory: apps/lootit
- Branch: feat/lootit-split
- Build: uses apps/lootit/Dockerfile (Railway auto-detects) or nixpacks as fallback
- Exposed port: 8080 (from Caddyfile `:8080`)

Required env vars for lootit-frontend service:
| Name | Value | Source |
|------|-------|--------|
| VITE_SUPABASE_URL | (same as frontend service) | Copy from existing frontend |
| VITE_SUPABASE_ANON_KEY | (same as frontend service) | Copy from existing frontend |
| VITE_API_BASE_URL | https://backend-production-58b4.up.railway.app | Fixed |
| VITE_PORTALIT_URL | https://portalit.gtools.io | Fixed per D-25 |

Backend env var to UPDATE (not create — already exists):
| Name | Old Value | New Value |
|------|-----------|-----------|
| CORS_ORIGIN | https://portalit.gtools.io,https://customer.portalit.gtools.io | https://portalit.gtools.io,https://customer.portalit.gtools.io,https://<lootit-frontend-railway-url> |

Railway MCP tool availability: If a Railway MCP tool is available (mcp__railway__*), use it for service creation and env-var management. Otherwise, the user creates the service via the Railway web dashboard. Either way, the plan surfaces the exact steps.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Commit phase 4 code, push feat/lootit-split branch, prepare for Railway service creation</name>
  <files>(git commits — no file modifications, just staging and pushing)</files>
  <read_first>
    - /Users/anielreyes/portalit/apps/lootit/ (verify scaffold + code from Plans 04-01, 04-02, 04-03 all exist)
    - /Users/anielreyes/portalit/src/api/client.js (verify Plan 04-04 edits landed)
    - /Users/anielreyes/portalit/src/lib/auth-storage.js (verify Plan 04-04 duplicate landed)
  </read_first>
  <action>
1. Confirm the branch state:
```bash
cd /Users/anielreyes/portalit && git status
cd /Users/anielreyes/portalit && git branch --show-current
```

2. If current branch is not `feat/lootit-split`, create it from production:
```bash
cd /Users/anielreyes/portalit && git checkout -b feat/lootit-split origin/production
```

3. Stage all phase 4 artifacts:
```bash
cd /Users/anielreyes/portalit && git add apps/lootit src/lib/auth-storage.js src/api/client.js
cd /Users/anielreyes/portalit && git status
```

4. Commit with a conventional-commits message:
```bash
cd /Users/anielreyes/portalit && git commit -m "feat: extract LootIT into apps/lootit/ with cookie auth adapter

- Scaffold apps/lootit/ as a self-contained Vite app (Plans 04-01, 04-02, 04-03)
- Add cookie+localStorage Supabase storage adapter for cross-subdomain SSO
- Duplicate adapter into portalit src/lib/auth-storage.js (D-19)
- Wire createAuthStorage into both api/client.js files (D-17)
- Existing portalit sessions preserved via backwards-compat read path (D-18)
"
```

5. Push the branch:
```bash
cd /Users/anielreyes/portalit && git push -u origin feat/lootit-split
```

Confirm the push succeeded (Railway needs the branch to exist on GitHub to deploy from it).
  </action>
  <acceptance_criteria>
    - `git branch --show-current` returns `feat/lootit-split`
    - `git log -1 --oneline` shows the phase 4 extraction commit
    - `git ls-remote origin feat/lootit-split` returns a ref (branch exists on origin)
    - `git status` shows clean working tree OR only untracked files unrelated to phase 4
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/anielreyes/portalit && [ "$(git branch --show-current)" = "feat/lootit-split" ] && git ls-remote origin feat/lootit-split | grep -q feat/lootit-split</automated>
  </verify>
  <done>feat/lootit-split branch pushed to origin with phase 4 commit containing apps/lootit/* and portalit auth-storage/client.js edits.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create the lootit-frontend Railway service and configure env vars</name>
  <what-built>
    - Phase 4 code committed to feat/lootit-split branch and pushed to origin
    - apps/lootit/Dockerfile + Caddyfile ready for Railway to build
  </what-built>
  <how-to-verify>
Claude cannot create the Railway service autonomously unless a Railway MCP tool is available. If `mcp__railway__*` tools are in the environment, use them. Otherwise the user performs these steps via the Railway web dashboard.

**If Railway MCP is available:**
1. Use `mcp__railway__create_service` (or similar) with:
   - project_id: `935a26e9-0cea-4eff-a7e7-d551840fad71`
   - name: `lootit-frontend`
   - source: github repo `AnielGammaTech/portalit`
   - branch: `feat/lootit-split`
   - root_directory: `apps/lootit`
2. Use `mcp__railway__set_variables` for the new service with:
   - VITE_SUPABASE_URL (copy from existing frontend service value)
   - VITE_SUPABASE_ANON_KEY (copy from existing frontend service value)
   - VITE_API_BASE_URL = `https://backend-production-58b4.up.railway.app`
   - VITE_PORTALIT_URL = `https://portalit.gtools.io`
3. Use `mcp__railway__generate_domain` to assign a public URL.

**If Railway MCP is NOT available, user performs these steps in the Railway web dashboard:**
1. Go to https://railway.app and open the PortalIT project.
2. Click "New Service" → "Deploy from GitHub repo".
3. Select repo `AnielGammaTech/portalit`.
4. Select branch `feat/lootit-split`.
5. Service name: `lootit-frontend`
6. Open service → Settings → "Root Directory" → set to `apps/lootit`. Click Save/Redeploy.
7. Open service → Variables → add these four variables:
   - `VITE_SUPABASE_URL` = (copy the exact value from the existing `frontend` service → Variables → VITE_SUPABASE_URL)
   - `VITE_SUPABASE_ANON_KEY` = (copy from `frontend` service)
   - `VITE_API_BASE_URL` = `https://backend-production-58b4.up.railway.app`
   - `VITE_PORTALIT_URL` = `https://portalit.gtools.io`
8. Open service → Settings → Domains → click "Generate Domain". Copy the resulting `*.up.railway.app` URL.
9. Wait for Railway to build and deploy (~2-5 minutes). Watch the Deployments tab for "Success".

Respond with:
- The generated Railway URL (e.g., `lootit-frontend-production-abcd.up.railway.app`)
- "Deployed successfully" or a paste of any build errors from Railway logs
  </how-to-verify>
  <resume-signal>Reply with the Railway URL and "deployed successfully", or paste the build error log.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Update backend service CORS_ORIGIN to include the new Railway URL</name>
  <what-built>
    - lootit-frontend service deployed at a Railway URL (from Task 2)
  </what-built>
  <how-to-verify>
Backend CORS must allow the new origin or `fetch` from lootit will be rejected with a CORS error in the browser.

**If Railway MCP is available:**
Use `mcp__railway__get_variables` on the `backend` service to read the current `CORS_ORIGIN` value. Then `mcp__railway__set_variables` with:
- `CORS_ORIGIN` = `<current-value>,https://<lootit-frontend-railway-url>`

**Otherwise, user performs via Railway web dashboard:**
1. Open the PortalIT project → backend service → Variables tab.
2. Find `CORS_ORIGIN`. It currently looks like `https://portalit.gtools.io,https://customer.portalit.gtools.io` (comma-separated).
3. Append the new URL: `https://portalit.gtools.io,https://customer.portalit.gtools.io,https://<lootit-frontend-railway-url-from-task-2>`
4. Save. Railway will restart the backend service automatically.
5. Wait ~30 seconds for the restart to complete.

**Security check:** Do NOT use a wildcard (`*`) or any host that's not under your control. Only append the specific Railway URL you got from Task 2. Per T-04-10 in Plan 04-04's threat model — CORS widening must be restrictive, not wildcard.

Respond with "CORS_ORIGIN updated" and paste the final comma-separated value (domains only, no secrets).
  </how-to-verify>
  <resume-signal>Reply with "CORS_ORIGIN updated" and the new value.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Smoke-test the deployed URL — HTTP reachability and bundle content</name>
  <files>(no files — runs curl against the deployed service)</files>
  <read_first>
    - (Task 2 response contains the Railway URL to test)
  </read_first>
  <action>
Using the Railway URL from Task 2, verify the service is reachable and serving the LootIT bundle:

```bash
LOOTIT_URL="<paste-from-task-2>"

# Root should return HTML with the React root div
curl -sS -o /tmp/lootit_root.html -w "HTTP %{http_code}\n" "$LOOTIT_URL/"

# Content should contain React root mount point
grep -q 'id="root"' /tmp/lootit_root.html && echo "root div: OK" || echo "root div: MISSING"

# SPA fallback should serve index.html for an arbitrary sub-path (Caddy try_files)
curl -sS -o /tmp/lootit_subpath.html -w "HTTP %{http_code}\n" "$LOOTIT_URL/customers/test-id"
grep -q 'id="root"' /tmp/lootit_subpath.html && echo "SPA fallback: OK" || echo "SPA fallback: MISSING"

# Assets directory should serve at least one hashed JS file (referenced from index.html)
ASSET=$(grep -oE '/assets/[^"]+\.js' /tmp/lootit_root.html | head -1)
if [ -n "$ASSET" ]; then
  curl -sS -o /dev/null -w "asset $ASSET: HTTP %{http_code}\n" "$LOOTIT_URL$ASSET"
fi
```

All three HTTP codes should be 200. If any returns non-200 or the "root div: MISSING" message appears, investigate the Railway deploy logs before proceeding.
  </action>
  <acceptance_criteria>
    - `curl -sS -o /tmp/lootit_root.html -w "%{http_code}" <LOOTIT_URL>/` returns 200
    - `/tmp/lootit_root.html` contains `id="root"`
    - SPA fallback path `/customers/test-id` returns 200 and contains `id="root"` (Caddy try_files working)
    - At least one hashed `/assets/*.js` referenced from index.html returns 200
  </acceptance_criteria>
  <verify>
    <automated>echo "Smoke test runs against the URL from Task 2 — this verify command is a no-op placeholder; the action's curl output is the real check"; true</automated>
  </verify>
  <done>The deployed URL serves LootIT's HTML shell with hashed assets; SPA fallback works for sub-paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual end-to-end SSO verification in Chrome</name>
  <what-built>
    - lootit-frontend deployed at a Railway URL
    - CORS_ORIGIN updated to allow it
    - HTML shell verified reachable
  </what-built>
  <how-to-verify>
This is the most important check in the phase — it proves the cookie SSO path works end-to-end for real.

**Prerequisites:** Portalit frontend service (portalit.gtools.io) must have the Plan 04-04 changes deployed. If it hasn't been redeployed yet since Plan 04-04 was committed, trigger a redeploy of the portalit `frontend` service from Railway dashboard → Deployments → "Redeploy" (Railway rebuilds the `production` branch, which must include the feat/lootit-split merge... actually, wait — per D-32, feat/lootit-split does NOT merge to production until phase 6. So portalit in prod is still on the OLD adapter.

**Corrected verification strategy:** Since portalit prod does not yet have the cookie adapter, the SSO cookie will not be set yet. This plan's SSO check is the ONE-WAY direction: lootit-frontend reads the cookie if portalit sets one. Since portalit isn't setting the cookie yet, the full SSO loop will not close in this plan.

Instead, verify what IS possible in wave 3:
1. Visit `<LOOTIT_URL>/` in Chrome incognito.
2. Expect: the page loads briefly, then redirects to `https://portalit.gtools.io/login?returnUrl=...` (RequireAuth per D-13). Confirm the returnUrl query param contains the lootit URL.
3. Log in to portalit with a normal account.
4. After login, you'll land on portalit dashboard (portalit's login form does not yet know how to honor returnUrl to external subdomains — this is expected).
5. Manually paste `<LOOTIT_URL>/` into the address bar.
6. **Expected BEHAVIOR (current state — backcompat path only):** lootit redirects you AGAIN to portalit login, because portalit prod has not yet written the session to cookie, only to localStorage. localStorage is origin-scoped, so lootit.up.railway.app cannot read portalit.gtools.io's localStorage.
7. **This is the expected failure mode before Plan 04-06 ships the cookie adapter to portalit prod.** Take a screenshot of the redirect loop — it IS the correct behavior for this wave.

**Alternative: test on a preview deploy of portalit with the feat/lootit-split branch**

To validate the cookie SSO loop closes BEFORE phase 6 merges to production, optionally:
1. Create a second Railway service (or reuse an existing staging if one exists) that deploys portalit `src/` from `feat/lootit-split` branch.
2. Visit that preview portalit, log in, confirm a `sb-*-auth-token` cookie appears on `.gtools.io` in DevTools → Application → Cookies.
3. Visit lootit URL — expect the cookie to be picked up and the user to be authenticated.
4. Since the preview portalit is on a different subdomain (e.g., `portalit-preview.up.railway.app`), the `.gtools.io` cookie won't be visible to a `.up.railway.app` URL. The full loop requires BOTH services to be on a shared registered domain.
5. If both services have `*.gtools.io` subdomains set (even temporary ones like `lootit-test.gtools.io` and `portalit-preview.gtools.io`), the loop closes.

**The minimum-viable verification for this plan:**
- lootit URL is reachable (Task 4 already verified)
- lootit unauthenticated redirect points to portalit login (RequireAuth working)
- The `api/client.js` in lootit does not crash on boot (i.e., createAuthStorage() constructs without error) — evidenced by the React page rendering at least the Loading… state

Steps:
1. Open `<LOOTIT_URL>/` in Chrome DevTools → Network tab enabled.
2. Expect a redirect to `portalit.gtools.io/login?returnUrl=...` — confirm in Network tab.
3. Open Console tab — confirm NO uncaught errors from `auth-storage.js` or `client.js`. Warnings about Supabase session fetches returning 401 before redirect are EXPECTED.
4. Go back to `<LOOTIT_URL>/` — expect the same redirect (idempotent).
5. In Safari, repeat the same steps and confirm it also redirects (Safari is sensitive to ITP; verify it doesn't crash the adapter).

Respond with:
- Chrome: redirect to portalit login works (yes/no)
- Chrome: no console errors from auth-storage (yes/no)
- Safari: same check (yes/no)
- Screenshot of the network tab showing the 200 on /, then navigation to portalit login

**The FULL SSO loop will be verified in Plan 04-07 after DNS cutover to lootit.gtools.io and portalit prod has the cookie adapter.**
  </how-to-verify>
  <resume-signal>Reply with "unauthenticated redirect works" or describe observed errors.</resume-signal>
</task>

</tasks>

<verification>
1. feat/lootit-split branch pushed with phase 4 commits.
2. lootit-frontend Railway service exists in PortalIT project, builds from apps/lootit/, has all four required env vars.
3. Backend service CORS_ORIGIN env var updated to include the new Railway URL (no wildcards).
4. `curl <LOOTIT_URL>/` returns 200 with an HTML shell containing `id="root"`.
5. SPA fallback works (sub-paths serve index.html via Caddy try_files).
6. In Chrome: unauthenticated visit to lootit URL redirects to portalit login with a returnUrl query param.
7. No uncaught errors from auth-storage.js when the page boots.
</verification>

<success_criteria>
- lootit-frontend is a live, addressable Railway service.
- The cookie adapter constructs cleanly in production (proven by the page rendering at least the redirect).
- Plan 04-06 can proceed to point portalit's sidebar at this URL.
- Plan 04-07 can proceed to add custom domain lootit.gtools.io.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-05-SUMMARY.md` with:
- Final Railway URL for lootit-frontend (exact string — subsequent plans reference this)
- Railway service ID (if visible from dashboard or MCP)
- Confirmed env var list on the new service (names only, no values)
- Backend CORS_ORIGIN final value (domains only, no secrets)
- Curl smoke test results (HTTP codes + content checks)
- Chrome verification notes (redirect flow working?)
- Known limitation: full SSO loop not yet closable because portalit prod does not yet have the cookie adapter (awaiting Plan 04-06 merge + Plan 04-07 DNS)
- Explicit note that Plan 04-06 will be wave 4 and Plan 04-07 will be wave 5
</output>
