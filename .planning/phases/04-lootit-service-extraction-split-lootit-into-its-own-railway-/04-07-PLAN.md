---
phase: 04-lootit-service-extraction
plan: 07
type: execute
wave: 5
depends_on: ["04-06"]
files_modified: []
autonomous: false
requirements: []
decision_ids: [D-24, D-25, D-29]
user_setup:
  - service: railway
    why: "Add custom domain lootit.gtools.io to lootit-frontend service and update dependent env vars"
    env_vars:
      - name: VITE_LOOTIT_URL
        source: "Updated on portalit frontend service: https://lootit.gtools.io"
    dashboard_config:
      - task: "Add lootit.gtools.io custom domain to lootit-frontend service"
        location: "Railway → PortalIT → lootit-frontend service → Settings → Domains → Add Custom Domain"
      - task: "Update backend service CORS_ORIGIN to include lootit.gtools.io"
        location: "Railway → backend service → Variables → CORS_ORIGIN"
      - task: "Update portalit frontend VITE_LOOTIT_URL to https://lootit.gtools.io and redeploy"
        location: "Railway → frontend service → Variables → VITE_LOOTIT_URL"
  - service: dns
    why: "Add CNAME record for lootit.gtools.io pointing at Railway"
    dashboard_config:
      - task: "Add CNAME record lootit.gtools.io → <railway-cname-target>"
        location: "User's DNS provider (gtools.io is user-managed per D-29)"

must_haves:
  truths:
    - "https://lootit.gtools.io resolves and serves HTTPS with a valid cert"
    - "Visiting lootit.gtools.io while signed in at portalit.gtools.io auto-authenticates (SSO loop closes)"
    - "Sign-out on either subdomain signs out on the other"
    - "The portalit sidebar LootIT link points at https://lootit.gtools.io (not the Railway *.up.railway.app URL)"
    - "Backend CORS_ORIGIN allows lootit.gtools.io"
  artifacts:
    - path: "(DNS record + Railway domain config — no local files)"
      provides: "Custom domain lootit.gtools.io on lootit-frontend service"
  key_links:
    - from: "lootit.gtools.io (DNS)"
      to: "lootit-frontend Railway service"
      via: "CNAME record"
    - from: "portalit.gtools.io cookie (Domain=.gtools.io)"
      to: "lootit.gtools.io browser context"
      via: "browser sends .gtools.io cookies to any subdomain"
---

<objective>
Add `lootit.gtools.io` as a custom domain on the lootit-frontend Railway service. Update backend CORS_ORIGIN to include the new domain. Update portalit's VITE_LOOTIT_URL env var to point at the final domain and redeploy. Run the full end-to-end SSO verification in Chrome and Safari.

Purpose: D-29 (custom domain cutover), D-24 (CORS allowlist finalization), D-25 (portalit env var pointed at final URL). This is the phase 4 cutover — the point at which LootIT is live at its real URL and phase 4 is DONE.

Output: A fully working lootit.gtools.io with SSO loop closed. Phase 4 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

> All git operations in this plan are performed within the `/gsd-execute-phase` workflow that invoked it, satisfying the CLAUDE.md GSD Workflow Enforcement rule.
</execution_context>

<context>
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-CONTEXT.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-RESEARCH.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-05-SUMMARY.md
@.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-06-SUMMARY.md

<interfaces>
DNS record to add (user-managed per D-29):
- Record type: CNAME
- Name: `lootit` (or `lootit.gtools.io` depending on DNS provider UI)
- Value/Target: (provided by Railway when the custom domain is added — typically `<something>.up.railway.app` or a CNAME flattening target)
- TTL: 300 (5 min) — low initially so we can revert quickly if needed

Railway custom domain flow:
1. Railway → lootit-frontend service → Settings → Domains → "Add Custom Domain" → enter `lootit.gtools.io`
2. Railway returns a CNAME target (e.g., `lootit-frontend-production-abcd.up.railway.app` or a generic `.up.railway.app`)
3. User adds that CNAME in DNS provider
4. Wait for DNS propagation + Railway TLS cert issuance (typically <5 min via Let's Encrypt)
5. Railway dashboard shows the domain as "Active" once cert is issued

Env var updates needed AFTER DNS is live:
- lootit-frontend service: no changes needed (VITE_PORTALIT_URL already points at portalit.gtools.io)
- portalit frontend service: update VITE_LOOTIT_URL from the Railway `.up.railway.app` URL to `https://lootit.gtools.io`
- backend service: update CORS_ORIGIN from `...,https://<railway-url>` to `...,https://lootit.gtools.io` (or keep both during transition)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Add lootit.gtools.io custom domain in Railway and user adds DNS CNAME</name>
  <what-built>
    - Plan 04-05 deployed lootit-frontend to a Railway `.up.railway.app` URL
    - Plan 04-06 merged to production and pointed portalit sidebar at that URL
  </what-built>
  <how-to-verify>
**Step A — Add custom domain in Railway:**

**If Railway MCP is available:** Use `mcp__railway__add_custom_domain` (or similar) on the lootit-frontend service with domain `lootit.gtools.io`. Capture the returned CNAME target.

**Otherwise via Railway dashboard:**
1. Open Railway → PortalIT → lootit-frontend service → Settings → Domains
2. Click "Add Custom Domain" (or "+ Custom Domain")
3. Enter: `lootit.gtools.io`
4. Railway will display a CNAME target to add in your DNS. Copy the EXACT value (e.g., `abc123.up.railway.app` or `cname.railway.app`). Paste it here in your response.

**Step B — User adds DNS CNAME:**

The user (Aniel) manages gtools.io DNS. Add this record at the DNS provider:
- Type: CNAME
- Host/Name: `lootit`
- Value: `<Railway CNAME target from Step A>`
- TTL: 300 (5 minutes — low for fast rollback)

Save the DNS record. Wait 30-120 seconds for propagation.

**Step C — Wait for Railway to issue a TLS certificate:**

Return to Railway → lootit-frontend → Settings → Domains. The `lootit.gtools.io` entry will go through states: "Waiting for DNS" → "Issuing cert" → "Active". This typically completes in under 5 minutes once DNS is live.

**Step D — Verify HTTPS reachability:**

```bash
curl -vI https://lootit.gtools.io/ 2>&1 | grep -E "HTTP|subject|issuer"
```
Expected: `HTTP/2 200` (or 301/302) and a valid TLS cert from Let's Encrypt / Railway.

Also run:
```bash
curl -sS https://lootit.gtools.io/ | grep -o 'id="root"' && echo "HTML served"
```
Should output: `id="root"` and `HTML served`.

Respond with:
- Railway CNAME target (the value you added in DNS)
- DNS record added and propagated (yes/no)
- TLS cert active in Railway (yes/no)
- `curl https://lootit.gtools.io/` returned 200 (yes/no)
  </how-to-verify>
  <resume-signal>Reply with "lootit.gtools.io is live" or describe the failure mode.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Update backend CORS_ORIGIN to allow lootit.gtools.io, update portalit VITE_LOOTIT_URL, and redeploy both</name>
  <what-built>
    - lootit.gtools.io serves HTTPS from the lootit-frontend Railway service
  </what-built>
  <how-to-verify>
**Step A — Update backend CORS_ORIGIN:**

The backend currently allows the Railway `.up.railway.app` URL (from Plan 04-05). Now add `https://lootit.gtools.io`.

**If Railway MCP is available:** Use `mcp__railway__get_variables` on backend, read current CORS_ORIGIN, then `mcp__railway__set_variables` to add `https://lootit.gtools.io` (keep the Railway URL too during the transition — it doesn't hurt).

**Otherwise via dashboard:**
1. Railway → PortalIT → backend service → Variables
2. Find CORS_ORIGIN — currently like `https://portalit.gtools.io,https://customer.portalit.gtools.io,https://<railway-url>`
3. Add `,https://lootit.gtools.io` to the end:
   `https://portalit.gtools.io,https://customer.portalit.gtools.io,https://<railway-url>,https://lootit.gtools.io`
4. Save. Backend auto-restarts in ~30 seconds.
5. Later (after ~1 week bake), remove the `<railway-url>` entry since traffic will all go via lootit.gtools.io. Not in scope for this task.

**Step B — Update portalit frontend VITE_LOOTIT_URL:**

**If Railway MCP is available:** `mcp__railway__set_variables` on frontend service, update VITE_LOOTIT_URL to `https://lootit.gtools.io`.

**Otherwise via dashboard:**
1. Railway → PortalIT → frontend service → Variables
2. Update `VITE_LOOTIT_URL` from the Railway `.up.railway.app` URL to `https://lootit.gtools.io`
3. Save. Railway will trigger a redeploy of the frontend service (because the env var changed, Vite must rebuild — RESEARCH Pitfall 4).
4. Wait for deploy (~3-5 minutes).

**Step C — Verify portalit sidebar now points at lootit.gtools.io:**

1. Visit `https://portalit.gtools.io` (incognito or fresh tab)
2. Sign in
3. Hover over the sidebar LootIT link
4. Confirm the tooltip/status bar shows `https://lootit.gtools.io`, NOT a `.up.railway.app` URL
5. Click the link — expect navigation to `https://lootit.gtools.io`

Respond with:
- Backend CORS_ORIGIN updated to include lootit.gtools.io (yes/no)
- Portalit VITE_LOOTIT_URL updated to https://lootit.gtools.io (yes/no)
- Portalit frontend redeployed successfully (yes/no)
- Sidebar LootIT link now points at https://lootit.gtools.io (yes/no, verified by hover)
  </how-to-verify>
  <resume-signal>Reply with "env vars updated, portalit redeployed" or describe errors.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full end-to-end SSO verification in Chrome and Safari — phase 4 gate</name>
  <what-built>
    - lootit.gtools.io live with TLS
    - Backend CORS allows lootit.gtools.io
    - Portalit frontend points sidebar LootIT at https://lootit.gtools.io
    - Cookie adapter deployed on both portalit prod (Plan 04-04) and lootit prod (Plan 04-05)
  </what-built>
  <how-to-verify>
This is the phase 4 acceptance gate. The SSO loop must close end-to-end.

**Chrome verification:**

1. Open Chrome in incognito mode. Visit `https://portalit.gtools.io`. Sign in with a normal account.
2. Open DevTools → Application → Cookies → `https://portalit.gtools.io`. Confirm there is a cookie named `sb-rgsvvywlnnkckvockdoj-auth-token` (or similar Supabase-prefixed name) with:
   - Domain: `.gtools.io` (note the leading dot)
   - Path: `/`
   - SameSite: `Lax`
   - Secure: `true`
   - HttpOnly: `false` (JS must read it — per RESEARCH D-18)
3. In the same incognito window, click the sidebar LootIT link (or manually navigate to `https://lootit.gtools.io`).
4. **EXPECTED:** The page loads the LootIT dashboard WITHOUT redirecting to login. The user is authenticated.
5. Open DevTools → Application → Cookies → `https://lootit.gtools.io`. Confirm the SAME `sb-*-auth-token` cookie is visible here (browser sends it because Domain=.gtools.io).
6. Navigate to a customer detail page in lootit. Confirm data loads from the API (Network tab shows 200 responses).
7. Click "Sign out" in lootit's Layout. Confirm you're redirected to portalit login.
8. In the same incognito, visit `https://portalit.gtools.io` — confirm you're signed out there too (onAuthStateChange propagated SIGNED_OUT).

**Safari verification:**

1. Open Safari in private window. Visit `https://portalit.gtools.io`. Sign in.
2. In DevTools (Develop menu) → Storage → Cookies → confirm the `.gtools.io` cookie is present.
3. Visit `https://lootit.gtools.io`. Expect the same auto-auth behavior.
4. Note: Safari ITP (RESEARCH Pitfall 3) will cap this cookie at 7 days. This is accepted — not a phase 4 blocker. Confirm the cookie loads correctly TODAY.

**Backcompat verification (critical):**

1. In a third browser profile, have a user whose session was created BEFORE Plan 04-04 deployed (i.e., they have a `sb-*-auth-token` ONLY in localStorage, no cookie yet). Simulation: open a fresh Chrome, visit portalit.gtools.io WITHOUT signing in, manually set a `sb-*-auth-token` key in localStorage matching a known valid session format (or test with a real user who was logged in yesterday).
2. Hard-refresh portalit. Confirm they're still signed in (backcompat read from localStorage).
3. After any navigation, confirm a cookie IS now also set (Plan 04-04 adapter dual-writes).
4. Visit `https://lootit.gtools.io` — confirm auto-auth now works because the cookie is populated.

**API smoke test via lootit:**

1. On `https://lootit.gtools.io`, trigger a customer list fetch.
2. Network tab → check the request to `https://backend-production-58b4.up.railway.app/...`:
   - Origin header: `https://lootit.gtools.io`
   - Authorization header: `Bearer <jwt>` (from adapter-backed session)
   - Response: 200 with customer JSON
3. If response is 403/401 → CORS misconfigured or token not passed. Debug.
4. If response is CORS error in console → CORS_ORIGIN env var not updated or backend didn't restart.

**Phase 4 acceptance criteria (all must be YES):**
- [ ] Chrome: SSO loop closes (portalit login → lootit auto-auth) — YES/NO
- [ ] Chrome: sign-out on lootit signs out portalit — YES/NO
- [ ] Safari: SSO loop closes today (ITP cap is acceptable) — YES/NO
- [ ] Backcompat: existing localStorage session still works after adapter swap — YES/NO
- [ ] API calls from lootit reach backend with correct CORS + auth — YES/NO

If ANY are NO → phase 4 is not complete. Debug the specific failure.

Respond with the checklist above filled in.
  </how-to-verify>
  <resume-signal>Reply with the checklist filled in (all YES = phase 4 done). If any NO, describe the failure.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Lower DNS TTL back to a normal value (optional finalization)</name>
  <files>(DNS record — no local files)</files>
  <read_first>
    - (no files — this is a manual operational task)
  </read_first>
  <action>
Once the SSO verification passes and you're confident in the cutover (24+ hours of stability recommended but not required), raise the DNS TTL for `lootit.gtools.io` CNAME from 300 seconds to a normal value (3600 or 14400 seconds). This reduces DNS query load once the record is stable.

This is a user-performed task in the DNS provider. Optional — the low TTL is harmless long-term, just slightly inefficient.

Respond with "TTL raised" or "deferred".
  </action>
  <acceptance_criteria>
    - Operator has been informed of the option to raise TTL
    - No blocker — task is advisory
  </acceptance_criteria>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Operator acknowledged the TTL raise option. Task complete regardless of choice.</done>
</task>

</tasks>

<verification>
1. `curl -I https://lootit.gtools.io/` returns 200 with valid TLS cert (not Railway-provisioned self-signed).
2. Chrome SSO loop closes: login on portalit.gtools.io → visit lootit.gtools.io → auto-authenticated, no redirect.
3. Safari SSO loop closes today (accepting the 7-day ITP cap).
4. Backcompat: existing portalit users with localStorage-only sessions remain signed in after adapter swap.
5. Sign-out from lootit propagates to portalit and vice versa.
6. API calls from lootit.gtools.io to backend succeed with correct CORS.
7. Portalit sidebar link points at https://lootit.gtools.io (not the Railway URL).
</verification>

<success_criteria>
- Phase 4 is DONE. LootIT is a live, separate Railway frontend service at lootit.gtools.io.
- Hard isolation achieved: PortalIT redesign work can now happen without any risk to LootIT.
- User can visit lootit.gtools.io in a fresh browser, log in once on portalit.gtools.io, and enjoy SSO to lootit.
- The source files in src/components/lootit/* remain for rollback (to be cleaned up in a separate cleanup phase after ~2 weeks of stability per D-35).
</success_criteria>

<output>
After completion, create `.planning/phases/04-lootit-service-extraction-split-lootit-into-its-own-railway-/04-07-SUMMARY.md` with:
- Railway CNAME target used for DNS
- Confirmation that lootit.gtools.io serves HTTPS with a valid cert
- Final CORS_ORIGIN env var on backend service
- Final VITE_LOOTIT_URL env var on portalit frontend service
- Chrome SSO loop closure (yes/no)
- Safari SSO loop closure (yes/no)
- Backcompat localStorage-session result (yes/no)
- Sign-out cross-propagation result (yes/no)
- Any observed regressions or edge cases

Also update `.planning/ROADMAP.md` to mark Phase 4 as complete and update `.planning/STATE.md` with the phase 4 finished status.

Also append a note to the phase directory as a marker for the followup cleanup tasks deferred per CONTEXT.md:
- Remove localStorage dual-write from auth-storage.js (after ~1 week bake)
- Delete src/components/lootit/*, src/hooks/useReconciliation*.js, src/lib/lootit-reconciliation.js from portalit (after ~2 weeks stable)
- These followups are separate from phase 4 and do NOT block the phase 4 DONE status.
</output>
