interface:
  display_name: "Portalit"
  short_description: "Repo-specific patterns and workflows for portalit"
  default_prompt: "Use the portalit repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true